# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /api/v1/organizations/:orgName         v1.organizations.ContributorsController.index(request:Request, orgName:String)
